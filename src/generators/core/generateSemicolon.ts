import { SemicolonStatement } from '../../types';

/**
 * Semicolon generator
 * Handles semicolon generation
 */
export function generateSemicolon(_semicolon: SemicolonStatement): string {
  return ';';
}